@page
@model TestesDePerformance1.Pages.Marca.EditModel

@{
    ViewData["Title"] = "Edit";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h1>Edit</h1>

<h4>Marcas</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Marcas.MarcaId" />
            <div class="form-group">
                <label asp-for="Marcas.DescricaoMarca" class="control-label"></label>
                <input asp-for="Marcas.DescricaoMarca" class="form-control" />
                <span asp-validation-for="Marcas.DescricaoMarca" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
